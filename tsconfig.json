{
  "compilerOptions": {
    "baseUrl": ".", // This must be specified if "paths" is.
    "paths": {
      "*": [
        "*",
        "src/*"
      ]
    },
    "allowJs": true,
    "target": "es6",
    "sourceMap": true,
    "module": "commonjs"

  },
  "exclude": [
    "node_modules",
    "dist"
  ]
}
